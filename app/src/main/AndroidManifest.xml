<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lianliankan"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
         <activity
            android:name="lianliankan.Loading"
            android:label="lianliankan"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">  
 			<intent-filter>
 			    <action android:name="android.intent.action.MAIN"/>
 			    <category android:name="android.intent.category.LAUNCHER"/>  
 			 </intent-filter>            
        </activity>
        <activity
            android:name="lianliankan.Link"
            android:label="lianliankan">
            </activity>
        <activity
            android:name="lianliankan.Building"
            android:label="lianliankan">
            </activity>
        <activity
            android:name="lianliankan.Welcome"
            android:label="lianliankan">
            </activity>
       <activity
            android:name="lianliankan.Range"
            android:label="lianliankan">
            </activity>
    </application>

</manifest>
